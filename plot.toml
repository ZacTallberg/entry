# Greenhouse Plot manifest — the homebase REGISTRY reads these to list every created project.
name = "entry"
display = "The Entry"
slug = "entry"
url = "https://entry.zacoberg.com"
ring = "blooming"            # seed | sprouting | blooming
public = true
auth_posture = "public-read, token-gated hub writes (X-Write-Token)"
network_info = "none — public-safe by construction"
summary = "Say something into the dark and the dark answers — in one of fifty forms, chosen by listening to how you said it — then it disappears without being kept."
